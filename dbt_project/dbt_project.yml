name: dbt_project
version: 1.0.0
profile: postgres_env

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]

models:
  staging:
    +materialized: view
  compute:
    +materialized: table
  published:
    +materialized: table
